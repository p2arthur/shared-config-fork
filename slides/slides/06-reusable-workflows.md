---
layout: section
---

# Reusable Workflows

The entry points for your CI/CD pipelines

<!--
Workflows are what consuming repos call via `uses:`. They're the public API. Don't conflate these with actions, which are internal implementation details. Consumers shouldn't need to know about individual actions; they just call workflows. Let me walk through the five workflows we provide and what each one does.
-->
